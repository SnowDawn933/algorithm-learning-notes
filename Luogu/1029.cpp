#include <stdio.h>
int gcd(int a, int b);
int main(void)
{
    int p, q, x, y;
    scanf("%d%d", &x, &y);
    for (int i = 1; i <= y; i++)
    {
        if (i % y == 0)
        {
             
        }
    }
}